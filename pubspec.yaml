name: introduction
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_slidable: ^3.0.0
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1 # Add this line

flutter:
  uses-material-design: true
  assets:
    # Uncomment if you plan to use assets
    # - images/a_dot_burr.jpeg
    # - images/a_dot_ham.jpeg

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png" # Path to your app icon
